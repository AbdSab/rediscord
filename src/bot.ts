import Discord from 'discord.js';
import config from '../config.json';

export class Bot{
    token: string;
    
    public constructor(token: string) {
        this.token = token;
    }

    public start(){
        
    }
}
